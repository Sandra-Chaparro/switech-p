---
import Layout from '../../../layouts/Layout.astro';
import BlogList from '../../sections/blog/BlogList.astro';
import Pagination from '../../common/Pagination.astro';
import type { Page } from "astro";
import type { CollectionEntry } from "astro:content";

export interface Props {
  page: Page<CollectionEntry<"blogs">>;
}

const { page } = Astro.props;
---

<Layout title="Blog" description="Technical articles on software engineering, DevSecOps, cybersecurity, and web development by Sandra Chaparro.">
  <BlogList blogs={page?.data || []} />
  <Pagination page={page} />
</Layout>
