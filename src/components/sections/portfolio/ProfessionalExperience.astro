---
import SectionHeading from '../../common/SectionHeading.astro';
import CardListSection from './CardListSection.astro';
import { experiences } from '../../../config/cv.json';

const items = experiences.map((exp) => ({
	title: exp.title,
	sub_title: exp.company,
	years: exp.years,
	bullets: exp.bullets,
	tech: exp.tech,
}));
---

<section id="experience" class="scroll-mt-20 py-8">
	<div class="container mx-auto px-4 max-w-5xl">
		<SectionHeading
			eyebrow="Career"
			title="Professional Experience"
			subtitle="A summary of my experience — full details are in my resume below."
		/>
		<CardListSection items={items} />
	</div>
</section>
